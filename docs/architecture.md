# Architecture Overview

## Data Flow
┌─────────────────┐ ┌──────────────┐ ┌─────────────────┐
│ POS/CRM/ERP │───▶│ GCS Raw │───▶│ Dataflow Batch │
│ (CSV/JSON) │ │ (bronze) │ │ (Beam Python) │
└─────────────────┘ └──────────────┘ └───────────────┐
│
┌─────────────────┐ ┌──────────────┐ ▼
│ Web/App │───▶│ Pub/Sub │───▶│Dataflow Stream │──▶
│ Events (JSON) │ │ (events) │ │ (Beam Python) │ │
└─────────────────┘ └──────────────┘ └───────────────┘ ▼
│ ┌──────────────┐
▼ │ BigQuery │
┌──────────────┐│ Curated │
│ Cloud Composer││ (silver/gold)│
│ (Airflow) ││ partitioned │
└──────────────┘└─┼─clustered │
│ PowerBI/Tableau│
└──────────────┘

## Technologies
- **Ingestion**: GCS, Pub/Sub
- **Processing**: Dataflow (Apache Beam Python)
- **Storage**: BigQuery (partitioned + clustered tables)
- **Orchestration**: Cloud Composer (Airflow)
- **IaC**: Terraform + Cloud Build CI/CD